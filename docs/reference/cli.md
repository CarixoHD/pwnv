<!--
Generated by scripts/gen_cli_docs.py - do not edit.
Change the Typer commands in pwnv/cli/ and run `python scripts/gen_cli_docs.py`.
-->

# Command Reference

Every command, generated from the CLI itself. `pwnv <command> --help` prints the
same thing in the terminal.


## Top level

### `pwnv doctor`

Checks configuration, paths, tools, and workspace consistency.

```
pwnv doctor [OPTIONS]
```

### `pwnv init`

Initializes a new pwnv environment, setting up the necessary directories and virtual environment.

```
pwnv init [OPTIONS]
```

| Option | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `--yes`, `-y` |  |  | Accept directory creation prompts for unattended setup |
| `--no-install`, `-n` |  |  | Do not install default packages in the virtual environment |
| `--no-examples` |  |  | Do not copy the bundled example plugins and templates |
| `--ctfs-folder`, `-f` | `PATH` | `./CTF` | Directory that will store all CTFs |
| `--python`, `-p` | `STR` | `3.13` | Python version or interpreter for the CTF environment |

### `pwnv reset`

Resets the pwnv environment by removing all CTFs and configuration files, with an option to create a backup.

```
pwnv reset [OPTIONS]
```

| Option | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `--force`, `-f` |  |  | Force reset without confirmation |

### `pwnv shell-init`

Prints the shell functions that add `pwncd` to your session. Add `eval "$(pwnv shell-init)"` to your shell's rc file, or `pwnv shell-init | source` for fish.

```
pwnv shell-init [OPTIONS]
```

| Option | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `--shell` | `STR` |  | bash, zsh, or fish (default: taken from $SHELL) |

### `pwnv solve`

Marks a challenge as solved, optionally submitting the flag to a remote CTF and adding tags.

```
pwnv solve [OPTIONS]
```

| Option | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `--flag` | `STR` | `` |  |
| `--challenge` | `STR` |  | Challenge name (skips selection) |
| `--tags` | `STR` |  | Comma-separated tags (skips prompt) |
| `--ctf` | `STR` |  | Limit selection to one CTF |
| `--history` |  |  | Show submission history |
| `--show-flags` |  |  | Show stored flags in submission history |

### `pwnv status`

Shows challenge and point progress for the workspace.

```
pwnv status [OPTIONS]
```

| Option | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `--ctf` | `STR` |  | Show one CTF only |
| `--detail`, `-d` |  |  | Add per-category progress, recent solves, and what is left |
| `--limit` | `INT RANGE` | `5` | Rows to show in the detail tables |
| `--json` |  |  | Print the result as JSON on stdout instead of a rendered view |

## `pwnv challenge`

### `pwnv challenge add`

Adds a new challenge to a selected CTF.

```
pwnv challenge add [OPTIONS] {name}
```

| Argument | Type | Required |
| :--- | :--- | :--- |
| `NAME` | `STR` | yes |

| Option | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `--ctf` | `STR` |  | CTF name (skips selection) |
| `--category` | `STR` |  | Challenge category (skips selection) |

### `pwnv challenge remove`

Removes an existing challenge from a CTF.

```
pwnv challenge remove [OPTIONS]
```

| Option | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `--challenge` | `STR` |  | Challenge name (skips selection) |
| `--ctf` | `STR` |  | Limit selection to one CTF |
| `--yes`, `-y` |  |  | Skip the confirmation prompt |

### `pwnv challenge info`

Displays detailed information about a selected challenge.

```
pwnv challenge info [OPTIONS]
```

| Option | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `--all`, `-a` |  |  | Show challenges from all CTFs |
| `--challenge` | `STR` |  | Challenge name (skips selection) |
| `--ctf` | `STR` |  | Limit selection to one CTF |
| `--json` |  |  | Print the result as JSON on stdout instead of a rendered view |

### `pwnv challenge filter`

Filters and displays solved challenges based on selected tags.

```
pwnv challenge filter [OPTIONS]
```

### `pwnv challenge search`

Search challenge names, descriptions, categories, and tags.

```
pwnv challenge search [OPTIONS] [query]
```

| Argument | Type | Required |
| :--- | :--- | :--- |
| `QUERY` | `STR` | no |

| Option | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `--ctf` | `STR` |  | Limit results to one CTF |
| `--category` | `STR` |  |  |
| `--tag` | `STR` |  | Required tag; repeatable |
| `--min-points` | `INT RANGE` |  |  |
| `--max-points` | `INT RANGE` |  |  |
| `--has-service`, `--no-service` |  |  | Filter by remote service availability |
| `--solved`, `--unsolved` |  |  | Filter by solve state |
| `--json` |  |  | Print the result as JSON on stdout instead of a rendered view |

### `pwnv challenge scaffold`

Re-run a plugin's template and setup for an existing challenge.

The plugin is chosen independently of the challenge, so you can drop the pwn template into a web challenge. Nothing about the challenge itself changes.

```
pwnv challenge scaffold [OPTIONS]
```

| Option | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `--challenge` | `STR` |  | Challenge name (skips selection) |
| `--ctf` | `STR` |  | Limit selection to one CTF |
| `--category` | `STR` |  | Template category to apply (defaults to the challenge's own) |
| `--plugin` | `STR` |  | Apply a specific plugin by name |
| `--force`, `-f` |  |  | Overwrite files that already exist |
| `--suffix` | `STR` | `` | Append to every written filename, e.g. --suffix _pwn -> solve_pwn.py |

### `pwnv challenge path`

Print a challenge directory and nothing else.

Built for `cd "$(pwnv challenge path baby-rop)"`; see `pwnv shell-init` for the `pwncd` wrapper.

```
pwnv challenge path [OPTIONS] [challenge_name]
```

| Argument | Type | Required |
| :--- | :--- | :--- |
| `CHALLENGE_NAME` | `STR` | no |

| Option | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `--ctf` | `STR` |  | Limit selection to one CTF |

### `pwnv challenge env add`

Installs packages into a challenge-local virtual environment.

```
pwnv challenge env add [OPTIONS] {packages}...
```

| Argument | Type | Required |
| :--- | :--- | :--- |
| `PACKAGES` | `STR` | yes |

| Option | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `--challenge` | `STR` |  |  |
| `--ctf` | `STR` |  |  |

### `pwnv challenge env run`

Runs a command with the challenge environment on PATH.

```
pwnv challenge env run [OPTIONS]
```

| Option | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `--challenge` | `STR` |  |  |
| `--ctf` | `STR` |  |  |

### `pwnv challenge note add`

Appends a timestamped entry to a challenge's NOTES.md file.

```
pwnv challenge note add [OPTIONS] {text}
```

| Argument | Type | Required |
| :--- | :--- | :--- |
| `TEXT` | `STR` | yes |

| Option | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `--section`, `-s` | `STR` | `Findings` |  |
| `--challenge` | `STR` |  |  |
| `--ctf` | `STR` |  |  |

### `pwnv challenge note show`

Renders a challenge's NOTES.md file.

```
pwnv challenge note show [OPTIONS]
```

| Option | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `--challenge` | `STR` |  |  |
| `--ctf` | `STR` |  |  |

## `pwnv ctf`

### `pwnv ctf add`

Adds a new CTF, either local or remote, to your environment.

```
pwnv ctf add [OPTIONS] {name}
```

| Argument | Type | Required |
| :--- | :--- | :--- |
| `NAME` | `STR` | yes |

| Option | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `--local` |  |  | Create a local CTF without prompting |
| `--url` | `STR` |  | Remote CTF URL (skips the remote prompt) |
| `--platform` | `STR` |  | Force the platform instead of letting ctfbridge detect it |
| `--username` | `STR` |  | (env: `PWNV_CTF_USERNAME`) |
| `--password` | `STR` |  | (env: `PWNV_CTF_PASSWORD`) |
| `--token` | `STR` |  | (env: `PWNV_CTF_TOKEN`) |

### `pwnv ctf remove`

Removes an existing CTF and all its associated challenges.

```
pwnv ctf remove [OPTIONS]
```

| Option | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `--ctf` | `STR` |  | CTF name (skips selection) |
| `--yes`, `-y` |  |  | Skip the confirmation prompt |

### `pwnv ctf info`

Displays detailed information about a selected CTF.

```
pwnv ctf info [OPTIONS]
```

| Option | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `--ctf` | `STR` |  | CTF name (skips selection) |
| `--json` |  |  | Print the result as JSON on stdout instead of a rendered view |

### `pwnv ctf stop`

Marks a running CTF as stopped.

```
pwnv ctf stop [OPTIONS]
```

| Option | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `--ctf` | `STR` |  | CTF name (skips selection) |

### `pwnv ctf start`

Marks a stopped CTF as running.

```
pwnv ctf start [OPTIONS]
```

| Option | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `--ctf` | `STR` |  | CTF name (skips selection) |

### `pwnv ctf sync`

Synchronizes challenges for a remote CTF.

```
pwnv ctf sync [OPTIONS]
```

| Option | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `--ctf` | `STR` |  | CTF name (skips selection) |
| `--watch`, `-w` |  |  | Keep polling for changes until you stop it |
| `--interval` | `INT RANGE` | `60` | Seconds between polls in --watch mode |
| `--refresh-attachments` |  |  | Re-download attachments even when the local copies still match |
| `--platform` | `STR` |  | Force the platform instead of letting ctfbridge detect it |

## `pwnv plugin`

### `pwnv plugin add`

Creates a new plugin file and its associated template for a specific category. Use 'pwnv plugin select' to activate it.

```
pwnv plugin add [OPTIONS] {name}
```

| Argument | Type | Required |
| :--- | :--- | :--- |
| `NAME` | `STR` | yes |

### `pwnv plugin remove`

Removes a selected plugin file and updates the plugin selection accordingly.

```
pwnv plugin remove [OPTIONS]
```

### `pwnv plugin info`

Lists all available plugins and displays detailed information, including source code, for a selected plugin.

```
pwnv plugin info [OPTIONS]
```

| Option | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `--json` |  |  | Print the result as JSON on stdout instead of a rendered view |

### `pwnv plugin select`

Selects the active plugin to be used for a chosen challenge category.

```
pwnv plugin select [OPTIONS]
```

## `pwnv workspace`

### `pwnv workspace backup`

Creates a full archive, including challenge files and credentials.

```
pwnv workspace backup [OPTIONS] [destination]
```

| Argument | Type | Required |
| :--- | :--- | :--- |
| `DESTINATION` | `PATH` | no |

| Option | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `--force`, `-f` |  |  |  |

### `pwnv workspace restore`

Restores a full backup archive: challenge files, notes, and credentials.

```
pwnv workspace restore [OPTIONS] {source}
```

| Argument | Type | Required |
| :--- | :--- | :--- |
| `SOURCE` | `PATH` | yes |

| Option | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `--force`, `-f` |  |  | Overwrite challenge files already on disk, and answer the --replace confirmation |
| `--replace` |  |  | Discard the current metadata instead of merging into it |

### `pwnv workspace export`

Exports portable metadata without challenge files or credentials.

```
pwnv workspace export [OPTIONS] [destination]
```

| Argument | Type | Required |
| :--- | :--- | :--- |
| `DESTINATION` | `PATH` | no |

### `pwnv workspace import`

Merges a portable export into the current workspace.

```
pwnv workspace import [OPTIONS] {source}
```

| Argument | Type | Required |
| :--- | :--- | :--- |
| `SOURCE` | `PATH` | yes |

| Option | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `--force`, `-f` |  |  | Answer the --replace confirmation |
| `--replace` |  |  | Discard the current metadata instead of merging into it |
